<div class="container">
  <div class="row">
    <div class="col-md-5 mx-auto p-2">
      <h1 class="h1 mt-5 mb-4 align-middle"><%= t('little_url.new.title') %></h1>
      <h4 class="h4"><%= t('little_url.new.subtitle') %></h4>
      <h6 class="h6"><%= t('little_url.new.subtitle2') %></h6>
      <%= render 'form' %>
    </div>
  </div>
  <%# render 'my_urls' %>
</div>
